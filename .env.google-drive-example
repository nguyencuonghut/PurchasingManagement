# ===========================================
# GOOGLE DRIVE BACKUP CONFIGURATION
# ===========================================
# Để sử dụng tính năng backup lên Google Drive (tương tự SqlBak)
# Bạn cần tạo OAuth 2.0 credentials tại Google Cloud Console

# Bước 1: Truy cập https://console.cloud.google.com/
# Bước 2: Tạo project hoặc chọn project có sẵn
# Bước 3: Bật Google Drive API trong "APIs & Services > Library"
# Bước 4: Tạo OAuth 2.0 Client ID trong "APIs & Services > Credentials"
# Bước 5: Thêm redirect URI: http://localhost:8000/auth/google-drive/callback
# Bước 6: Copy Client ID và Client Secret vào .env

# Google OAuth Client ID (từ Google Cloud Console)
GOOGLE_DRIVE_CLIENT_ID="your_google_client_id_here"

# Google OAuth Client Secret (từ Google Cloud Console)
GOOGLE_DRIVE_CLIENT_SECRET="your_google_client_secret_here"

# Google OAuth Redirect URI (phải khớp với Google Console)
GOOGLE_DRIVE_REDIRECT_URI="${APP_URL}/auth/google-drive/callback"

# ===========================================
# QUEUE CONFIGURATION (cho email notifications)
# ===========================================
QUEUE_CONNECTION=database

# ===========================================
# LƯUÝ: Sau khi setup, user chỉ cần:
# 1. Click "Connect to Google Drive"
# 2. Authorize ứng dụng
# 3. Chọn folder để lưu backup
# 4. Xong! (Đơn giản như SqlBak)
# ==========================================
